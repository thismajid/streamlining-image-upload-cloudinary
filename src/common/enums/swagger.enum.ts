export enum SwaggerDocument {
  TITLE = 'Streamlining Image Upload with Cloudinary',
  DESCRIPTION = 'Api for Streamlining Image Upload with Cloudinary',
  VERSION = 'v1',
  TAG = 'Streamlining Image Upload with Cloudinary',
  PATH = '/docs',
}

export enum SwaggerBearer {
  TYPE = 'http',
  SCHEMA = 'Bearer',
  BEARER_FORMAT = 'JWT',
  HEADER = 'header',
}

export enum SwaggerConsumes {
  JSON = 'application/json',
  FORM_URL_ENCODED = 'application/x-www-form-urlencoded',
  MULTIPART = 'multipart/form-data',
}
